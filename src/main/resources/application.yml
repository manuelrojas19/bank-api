server:
  port: 8080

spring:
  application:
    name: bank-api
  data:
    mongodb:
      database: atm_data_db
      port: 27017
      host: localhost

bank-api:
  information:
    host: localhost


